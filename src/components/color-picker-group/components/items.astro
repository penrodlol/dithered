---
import type { HTMLAttributes } from 'astro/types';
import { twMerge } from 'tailwind-merge';
import Item from './item.astro';

type Props = HTMLAttributes<'div'>;

const { class: className, ...props } = Astro.props;
---

<div
  {...props}
  x-ref="colorPickerGroupItems"
  class={twMerge(
    'scrollbar border-gray-6 flex size-full flex-wrap items-center gap-2 overflow-y-auto border-r border-dashed p-2',
    className,
  )}
>
  <template x-for="(color, index) in activePalette"><Item :data-index="index" /></template>
</div>
